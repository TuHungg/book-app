const initialAuth = {
  username: "",
  email: "",
};

function reducerAuth(state, action) {
  switch (action.type) {
    default:
      throw new Error();
  }
}

export { initialAuth, reducerAuth };
